%p#notice= notice
%h1 Listing Comments
%table
  %thead
    %tr
      %th Image url
      %th Body
      %th User
      %th Battle
      %th Comment
      %th{:colspan => "3"}
  %tbody
    - @comments.each do |comment|
      %tr
        %td= comment.image_url
        %td= comment.body
        %td= comment.user_id
        %td= comment.battle_id
        %td= comment.comment_id
        %td= link_to 'Show', comment
        %td= link_to 'Edit', edit_comment_path(comment)
        %td= link_to 'Destroy', comment, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Comment', new_comment_path
